# 文档维护规则

1. 只记录当前行为、架构边界和非显然的工程约束；历史计划、阶段记录和已失效结论交给 Git 历史。
2. 修改前核对相关代码与测试。同一主题只维护一份合同，其它文档引用它，不重复源码字段、依赖版本或工作流清单。
3. 正文使用中文，代码标识、协议名和平台 API 保留原文。GitHub 公开内容遵循根目录 `AGENTS.md`。
4. 文件名使用 kebab-case，链接使用仓库相对路径。检查本地链接与 `git diff --check`，文档改动不要求运行 App 测试或构建。
